---
id: password-change
title: passwordChange
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








Change a user's password. The client is required to compute authPW since we don't send the clear password to our server.


```graphql
passwordChange(
  input: PasswordChangeInput!
): PasswordChangePayload!
```




### Arguments

#### [<code style={{ fontWeight: 'normal' }}>passwordChange.<b>input</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>PasswordChangeInput!</b></code>](/gql-api/types/inputs/password-change-input.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="input"/> 



### Type

#### [<code style={{ fontWeight: 'normal' }}><b>PasswordChangePayload</b></code>](/gql-api/types/objects/password-change-payload.mdx) <Badge class="badge badge--secondary " text="object"/>